# node_modules/ 폴더는 npm으로 설치되는 패키지들이 포함되므로 필요 없음
node_modules/

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 환경 변수 파일 (API 키, 데이터베이스 설정 등 민감한 정보)
.env

# 캐시 및 빌드 디렉토리
.cache/
dist/
build/

# OS에서 생성하는 불필요한 파일들
.DS_Store
Thumbs.db

# npm 패키지 잠금 파일 (선택사항: 팀에 따라 무시할지 여부를 결정)
# package-lock.json
# yarn.lock

# IDE와 에디터 관련 설정 파일
.vscode/
.idea/

# 테스트 커버리지 결과 파일
coverage/

# pm2와 같은 프로세스 매니저의 설정 파일
pm2-log.json
pids/
logs/

# TypeScript 관련 (선택적으로 추가)
*.tsbuildinfo

# 기타 환경별 임시 파일
*.log
*.tmp
*.bak

# config
config/